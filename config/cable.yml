# development:
#   adapter: async

development:
  adapter: redis
  url: redis://localhost:6379/1
  channel_prefix: your_app_development

test:
  adapter: test

production:
  adapter: async

# production:
#   adapter: redis
#   url: <%= ENV['REDIS_URL'] %>
#   channel_prefix: asb-app-backend_production